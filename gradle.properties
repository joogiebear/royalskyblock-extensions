#libreforge-updater
version=2026.36.1
eco-version=2026.35
libreforge-version=2026.35.1
# Derived, not authored: tools/install-deps.ps1 writes this from the host source tree, and
# the CI workflows bypass it entirely by passing -Proyalskyblock-version from the host build
# they install. Present so a plain `gradlew build` works after install-deps has run.
royalskyblock-version=2026.36.0
paper-version=26.2.build.121-stable
kotlin.code.style=official
